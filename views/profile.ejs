<% layout('layout') %>

<div class="gap10 polaroid corner8">
    <section class="width30 bgWhite corner15 col padding20 shadow1 relative" id="infoCard">
        <a href="javascript:void(0)" id="lgBtn" class="nav cnav absolute right20 top20"><i class="fas fa-sign-out"></i></a>
        <img src="/images/profile.png" alt="" class="h130 w130 corner500">
        <p class="size28 str500"><%= user.fName %> <%= user.lName %></p>
        <p class="size16 str400"><%= user.role %> </p>
        <br>
        <section class="col  bgWhite corner10">
            <p class="size16 str400 alignStart"><%= user.email %></p>
            <p class="size16 str400"><%= user.phone %></p>
            <p class="size16 str400"><%= user.Department %></p>
        </section>
        <br>
        <section class="gap10 padding0">
            <a href="javascript:void(0)" class="nav width100 borderPrimary editBtn" id="editBtn"><i class="fas fa-pen"></i>Edit Info</a>
            <a href="javascript:void(0)" class="nav width100 borderPrimary keyBtn" id="keyBtn"><i class="fas fa-key"></i>Change Password</a>
        </section>
    </section>
    
    <section class="width30 bgWhite corner15 col padding20 shadow1 relative hidden" id="lgCard">
        <div href="" class="nav cnavLG"><i class="fas fa-sign-out"></i></div>
        <br>
        <p class="size18 textCenter textBalance">Are you sure you want to Log Out?</p>
        <br>
        <section class="padding0 justifyBetween gap10">
            <a href="javascript:void(0)" class="nav width100">Cancel</a>
            <a href="/" class="nav pnav width100">Log Out</a>
        </section>
    </section>

    
    <form class="width30 bgWhite corner15 col padding20 shadow1 gap10 hidden" id="editCard">
        <p class="size28 str500">Edit Information</p>
        <div class="field">
            <label for="">First Name</label>
            <input type="text">
        </div>
        <div class="field">
            <label for="">Middle Name</label>
            <input type="text">
        </div>
        <div class="field">
            <label for="">Last Name</label>
            <input type="text">
        </div>
        <div class="field">
            <label for="">Ext Name</label>
            <input type="text">
        </div>
        <br>
        <div class="field">
            <label for="">Email</label>
            <input type="text">
        </div>
        <div class="field">
            <label for="">School ID</label>
            <input type="text">
        </div>
        <div class="field">
            <label for="">Department</label>
            <div class="selectBar">
                <select name="" id="">
                    <option value="">CSDL</option>
                </select>
            </div>
        </div>
        <div class="row gap10">
            <a class="nav width100" id="closeEdit">Cancel</a>
            <button class="nav pnav width100 ">Update</button>
        </div>
    </form>

    
    
    <form class="width30 bgWhite corner15 col padding20 shadow1 gap10 hidden" id="passCard">
        <p class="size28 str500 ">Change Password</p>
        <p class="noteBlue width100">Instruction Here</p>
        <div class="field">
            <label for="">Current Password</label>
            <input type="password">
        </div>
        <div class="field">
            <label for="">Create Password</label>
            <input type="password">
        </div>
        <div class="field">
            <label for="">New Password</label>
            <input type="password">
        </div>
        <div class="row gap10">
            <a class="nav width100" id="closePassword">Cancel</a>
            <button class="nav pnav width100 ">Reset</button>
        </div>
    </form>

</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
    const info = document.getElementById('infoCard');
    const edit = document.getElementById('editCard');
    const pass = document.getElementById('passCard');
    const lg = document.getElementById('lgCard');

    // Simple function to switch views
    const show = (card) => {
        [info, edit, pass, lg].forEach(c => c.classList.add('hidden'));
        card.classList.remove('hidden');
    };

    // Open Forms
    document.getElementById('editBtn').onclick = (e) => { e.stopPropagation(); show(edit); };
    document.getElementById('keyBtn').onclick = (e) => { e.stopPropagation(); show(pass); };
    document.getElementById('lgBtn').onclick = (e) => { e.stopPropagation(); show(lg); };

    // Cancel / Close Buttons
    document.getElementById('closeEdit').onclick = () => show(info);
    document.getElementById('closePassword').onclick = () => show(info);

    // Click outside to return to infoCard
    document.onclick = (e) => {
        if (!edit.contains(e.target) && !pass.contains(e.target)) show(info);
    };

    // Prevent closing when clicking inside the forms
    [edit, pass].forEach(c => c.onclick = e => e.stopPropagation());
});
</script>